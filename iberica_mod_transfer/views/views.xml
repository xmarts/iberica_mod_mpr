<odoo>
  <data>
    <!-- explicit list view definition >

    <record model="ir.ui.view" id="herencia_module_stock">
      <field name="name">herencia list stok</field>
      <field name="model">product.template</field>
      <field name="inherit_id" ref="product.product_template_only_form_view" />
      <field name="arch" type="xml">

        <xpath expr="//field[@name='weight']" position = "before">
          <field name="peso_caja_presentacion"/>
		    </xpath>
      </field>
    </record-->

    <record model="ir.ui.view" id="herencia_module_sp">
      <field name="name">herencia list stok</field>
      <field name="model">stock.picking</field>
      <field name="inherit_id" ref="stock.view_picking_form" />
      <field name="arch" type="xml">

        <xpath expr="//field[@name='location_dest_id']" position = "before">
          <field name="relacion"/>
          <field name="peso_tara" attrs="{'invisible': ('picking_type_id', '!=', 765)}"/>
          <!--<field name="default" invisible="1"/>-->
          <field name="tara" invisible="1"/>
		    </xpath>

        <xpath expr="//field[@name='product_id']" position ="after">
          <field name="peso_tara" invisible="1" />
          <field name="peso_bruto" attrs="{'invisible': ('picking_id.relacion', '!=', 765)}"/>
          <field name="peso_neto" attrs="{'invisible': ('picking_id.relacion', '!=', 765)}"/>
          <field name="picking_relacion"/>
          <field name="peso_bruto_x" invisible="1"/>
          <field name="peso_neto_x" invisible="1"/>
          <field name="semielaborado" invisible="1"/>
          <field name="tara" invisible="1"/>
        </xpath>


        <!--xpath expr = "//field[@name='line_ids']/tree/field[@name='account_id']" position = "before" >
          <field name="unidad"/>
          <field name="metros"/>
          <field name="cajas"/>
         attrs="{'invisible': [('product_id.categ_id', '=', False)]}"
          <field name="unidad_x" invisible="1"/>
          <field name="metros_x" invisible="1"/>
          <field name="cajas_x" invisible="1"/>
        </xpath-->
      </field>
    </record>

  <record model="ir.ui.view" id="herencia_module_sp_line">
      <field name="name">herencia list stok</field>
      <field name="model">stock.move.line</field>
      <field name="inherit_id" ref="stock.view_move_line_tree" />
      <field name="arch" type="xml">

        <xpath expr="//field[@name='qty_done']" position = "before">
          <field name="peso_tara" invisible="1" />
          <field name="peso_bruto" attrs="{'invisible': ('picking_id.relacion', '!=', 765)}"/>
          <field name="peso_neto" attrs="{'invisible': ('picking_id.relacion', '!=', 765)}"/>
          <field name="picking_relacion"/>
          <field name="peso_bruto_x" invisible="1"/>
          <field name="peso_neto_x" invisible="1"/>
          <field name="semielaborado" invisible="1"/>
          <field name="tara" invisible="1"/>
        </xpath>
        <!--xpath expr = "//field[@name='line_ids']/tree/field[@name='account_id']" position = "before" >
          <field name="unidad"/>
          <field name="metros"/>
          <field name="cajas"/>
attrs="{'invisible': [('product_id.categ_id', '=', False)]}"
          <field name="unidad_x" invisible="1"/>
          <field name="metros_x" invisible="1"/>
          <field name="cajas_x" invisible="1"/>
        </xpath-->
      </field>
    </record>
  </data>
</odoo>
